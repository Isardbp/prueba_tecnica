export interface UserData {
    userName: string;
    mail: string;
    img: string;
}

export interface Product {
    id: number;
    name: string;
    price: number;
}

export interface Task {
    id: number;
    text: string;
  }